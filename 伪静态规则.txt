/inx伪静态开始  复制虚线以内的不含虚线
-----------------------------------------
location / {
   if (!-e $request_filename) {
   rewrite  ^(.*)$  /index.php?sb=$1  last;
   break;
    }
 }
  
 location ^~ /runtime {
   deny all;
}
  location ^~ /Runtime {
       deny all;
  }    
  location ^~ /cert {
       deny all;
   }
-----------------------------------------
//inx伪静态结束